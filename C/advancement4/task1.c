#include <stdio.h>

int main()
{
    int arr[5];
    int a;
    printf("gimme them numbers already 5 of them");
    for (i = 0; i < 5; i++)
    {
        scanf("%d", &arr[i]);
        printf("next");
    }
    return 0;
}